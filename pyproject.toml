[project]
name = "my-online-profile-backend"
version = "0.1.0"
description = "Backend API with Todo CRUD and RAG endpoint"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.7",
    "libsql-client>=0.3.1",
    "openai>=2.20.0",
    "uvicorn>=0.40.0",
]
